spring:
  datasource:
    url: jdbc:h2:mem:auth-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: raceflow-dev-secret-key-for-local-dev-only-32chars
  expiration: 86400000
